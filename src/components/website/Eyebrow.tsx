export default function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
   <p className="font-mono text-eyebrow-md font-semibold uppercase tracking-eyebrow text-atlas-blue">
    {children}
</p>
  );
}
